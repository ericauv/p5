const height = window.innerHeight;
const width = window.innerWidth;
const gridIntervalVertical = 10
const gridIntervalHorizontal = 10

function setup() {
    createCanvas(width, height);
    background(220);
    Grid(gridIntervalVertical, gridIntervalHorizontal);
  }
  
  function draw() {

  }
